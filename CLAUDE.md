# Claude Code 自动 Skills 调用规则

以下规则指导我在合适场景下自动调用对应的 skills，不需要用户手动触发。

## 技能自动触发场景

### cavecrew
- **触发场景**: 涉及大量代码搜索、跨多文件编辑、代码审查，或对话上下文已较长需要节省空间时
- **行为**: 自动委托子任务给 investigator / builder / reviewer 子代理

### karpathy-guidelines
- **触发场景**: 编写、审查、重构任何代码时
- **行为**: 参考其原则，避免过度设计、保持精准修改、明确成功标准

### ui-styling
- **触发场景**: 用户要求构建 UI 界面、组件、设计系统，涉及 Tailwind / shadcn/ui / Radix UI
- **行为**: 自动使用构建美观、可访问的 UI

### ui-ux-pro-max
- **触发场景**: 涉及 UI/UX 设计决策、网页/移动端设计、布局规范、色彩/字体选择
- **行为**: 利用其 50+ 设计风格和调色板/字体库提供专业设计方案

### claude-api
- **触发场景**: 代码中 import `anthropic` / `@anthropic-ai/sdk`；用户询问 Claude API / Anthropic SDK；涉及 prompt caching、thinking、tool use、batch 等特性
- **行为**: 自动使用构建、调试、优化 Claude API 应用

### security-review
- **触发场景**: 用户提到安全审查、漏洞分析、OWASP、渗透测试；或在提交/审查代码时涉及敏感操作
- **行为**: 自动执行安全审查

### review
- **触发场景**: 用户要求审查 PR、代码评审、审查变更
- **行为**: 自动调用进行 PR 审查

### simplify
- **触发场景**: 完成代码变更后，自动检查代码质量、可复用性和效率
- **行为**: 审查变更并在发现问题时修复

### loop
- **触发场景**: 用户需要定期轮询、持续监控、重复执行某个操作
- **行为**: 自动设置为按间隔循环执行

### update-config
- **触发场景**: 用户要求修改设置、权限、环境变量、钩子行为
- **行为**: 自动更新 settings.json

### keybindings-help
- **触发场景**: 用户想自定义键盘快捷键、修改键位绑定
- **行为**: 自动调用快捷键配置帮助

### fewer-permission-prompts
- **触发场景**: 用户频繁遇到权限提示，或明确要求减少权限弹窗
- **行为**: 扫描对话记录并将常用命令加入白名单

### design
- **触发场景**: 用户要求 Logo 设计、品牌设计、海报/横幅/图标生成、演示文稿
- **行为**: 调用设计技能生成对应素材

### init
- **触发场景**: 检测到新项目尚无 CLAUDE.md 且用户希望初始化项目文档
- **行为**: 自动初始化项目文档

### anthropic-skills:consolidate-memory
- **触发场景**: 发现记忆文件存在重复、过时或需要整理时
- **行为**: 自动整理和合并记忆

## 通用原则

1. **优先匹配**：以上规则按列表中从上到下的优先级匹配，首个匹配的场景优先
2. **不重复触发**：已在执行某个 skill 时，不再触发其他非必要的 skill
3. **用户可覆盖**：用户明确指定用或不用某个 skill 时，以用户指令为准
