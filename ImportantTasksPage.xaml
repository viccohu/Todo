<Page
    x:Class="Todo.ImportantTasksPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Todo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="#161616">

    <Grid Padding="40,20,40,40">
        <StackPanel MaxWidth="640" HorizontalAlignment="Stretch">
            <StackPanel Orientation="Horizontal" Spacing="15" Margin="0,0,0,30">
                <FontIcon Glyph="&#xE8C8;" FontSize="28" Foreground="White"/>
                <TextBlock Text="重要任务" FontSize="28" FontWeight="SemiBold" Foreground="White"/>
            </StackPanel>

            <ItemsControl ItemsSource="{Binding Tasks, Mode=OneWay}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#333" BorderThickness="1" CornerRadius="8" Height="60" Margin="0,0,0,10" Background="#1e1e1e">
                            <Grid>
                                <Rectangle Fill="{Binding DueUrgencyBackground}" RadiusX="8" RadiusY="8"/>
                                <Grid Margin="15,0">
                                    <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}" Click="TaskCheckBox_Click">
                                        <TextBlock Text="{Binding Title}" Foreground="White" Margin="5,0,0,0"/>
                                    </CheckBox>
                                    <TextBlock Text="{Binding DueDateDisplay}" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="#666" FontSize="12"/>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Button Margin="0,20,0,10" Background="#262626" BorderThickness="0" CornerRadius="4" Padding="8,4">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE787;" FontSize="12"/>
                    <TextBlock Text="已完成" FontSize="12"/>
                    <FontIcon Glyph="&#xE70D;" FontSize="10"/>
                </StackPanel>
            </Button>

            <ItemsControl ItemsSource="{Binding CompletedTasks, Mode=OneWay}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#333" BorderThickness="1" CornerRadius="8" Height="60" Margin="0,0,0,10" Background="#1e1e1e" Opacity="0.6">
                            <Grid>
                                <Rectangle Fill="{Binding DueUrgencyBackground}" RadiusX="8" RadiusY="8"/>
                                <Grid Margin="15,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="4"/>
                                    </Grid.ColumnDefinitions>
                                    <CheckBox Grid.Column="0" IsChecked="{Binding IsChecked, Mode=TwoWay}" Click="TaskCheckBox_Click">
                                        <TextBlock Text="{Binding Title}" TextDecorations="Strikethrough" Foreground="#888"/>
                                    </CheckBox>
                                    <TextBlock Text="{Binding DueDateDisplay}" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="#555" FontSize="12" Margin="0,0,15,0"/>
                                    <Rectangle Grid.Column="1"
                                               Fill="{Binding CompletionIndicatorBrush}"
                                               VerticalAlignment="Stretch"
                                               RadiusX="2" RadiusY="2"
                                               Margin="0,2"/>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Border BorderBrush="#333" BorderThickness="1" CornerRadius="8" Height="60" Margin="0,10,0,0">
                <Button Click="AddTask_Click" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Left">
                    <StackPanel Orientation="Horizontal" Spacing="12" Margin="10,0">
                        <FontIcon Glyph="&#xE710;" FontSize="18" Foreground="#0078d4"/>
                        <TextBlock Text="添加任务" Foreground="#0078d4" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Border>
        </StackPanel>
    </Grid>
</Page>
