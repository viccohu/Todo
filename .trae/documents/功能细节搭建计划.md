# 功能细节搭建实现计划

## 概述
本计划旨在实现任务管理的完整功能细节，包括截止时间、提醒时间、重复功能以及修复数据保存问题。

## 任务数据结构完善

### 现有字段
- ✓ 任务内容 (Title)
- ✓ 子任务 (SubTasks)
- ✓ 备注 (Description)
- ✓ 创建时间 (CreatedAt)
- ✓ 截止时间 (DueDate)
- ✓ 提醒时间 (Reminders - 需要扩展)
- ✓ 重复 (Recurrence - 需要完善)

### 需完善字段
1. **提醒时间 (Reminder)** - 需要扩展支持多日期提醒和多次提醒间隔
2. **重复 (Recurrence)** - 需要完善周期计算逻辑

## 功能实现详细步骤

### 1. 数据模型完善

#### 1.1 扩展 Reminder 模型
- 文件: `Models/Reminder.cs`
- 修改:
  ```csharp
  public class Reminder
  {
      public int Id { get; set; }
      public int TaskId { get; set; }
      public ReminderType ReminderType { get; set; }
      public DateTime? ReminderDateTime { get; set; }
      
      // 新增：支持多日期提醒（逗号分隔的日期字符串，如 "1,2,6" 表示1号、2号、6号）
      public string? CustomDays { get; set; }
      
      // 新增：当日多次提醒间隔（分钟），0表示不启用
      public int SameDayIntervalMinutes { get; set; }
      
      // 新增：是否启用多日期提醒
      public bool EnableMultiDayReminders { get; set; }
  }
  ```

#### 1.2 扩展 Recurrence 模型
- 文件: `Models/Recurrence.cs`
- 修改:
  ```csharp
  public class Recurrence
  {
      public int Id { get; set; }
      public int TaskId { get; set; }
      public RecurrenceType RecurrenceType { get; set; }
      public DateTime BaseDate { get; set; }
      
      // 新增：下次触发的截止日期
      public DateTime NextDueDate { get; set; }
  }
  ```

### 2. 数据库服务扩展

#### 2.1 扩展 Reminders 表
- 文件: `Services/DatabaseService.cs`
- SQL 变更:
  ```sql
  ALTER TABLE Reminders ADD COLUMN CustomDays TEXT;
  ALTER TABLE Reminders ADD COLUMN SameDayIntervalMinutes INTEGER DEFAULT 0;
  ALTER TABLE Reminders ADD COLUMN EnableMultiDayReminders INTEGER DEFAULT 0;
  ```

#### 2.2 添加/更新数据库方法
- `AddReminder` 方法需要支持新参数
- `GetRemindersForTask(int taskId)` - 获取任务的所有提醒
- `UpdateReminder(Reminder reminder)` - 更新提醒设置
- `DeleteRemindersForTask(int taskId)` - 删除任务的所有提醒

#### 2.3 扩展 Recurrences 表
- SQL 变更:
  ```sql
  ALTER TABLE Recurrences ADD COLUMN NextDueDate TEXT;
  ```

#### 2.4 添加/更新 Recurrence 方法
- `UpdateRecurrence(Recurrence recurrence)` - 更新重复设置
- `GetNextRecurrenceInfo(int taskId)` - 获取下次重复信息

### 3. UI 实现

#### 3.1 截止时间功能（CalendarDatePicker）
- 位置: `MainWindow.xaml` 第473行
- 修改:
  - 将 CalendarDatePicker 的 Visibility 改为默认不显示
  - 点击"截止日期"按钮时切换显示/隐藏
  - 选择日期后自动关闭 CalendarDatePicker
  - 自动保存到数据库

#### 3.2 提醒时间功能（弹窗）
- 位置: 需要创建新的 Flyout 或 ContentDialog
- 功能:
  1. **顶部**: CalendarView 允许多选（只允许选到截止日期前）
  2. **中部**: TimePicker 选择提醒时间
  3. **底部**: "多次提醒"开关 + 下拉选项
     - 10分钟、20分钟、30分钟、1小时、2小时、3小时、5小时、6小时

- 布局结构:
  ```xml
  <StackPanel Width="320" Padding="16">
    <!-- 日历多选区 -->
    <CalendarView x:Name="ReminderCalendar"
                   SelectionMode="Multiple"
                   MaxDate="{Binding DueDate}"
                   SelectedDatesChanged="ReminderCalendar_SelectedDatesChanged"/>
    
    <!-- 时间选择 -->
    <TimePicker x:Name="ReminderTimePicker"
                Header="提醒时间"
                Margin="0,12,0,0"/>
    
    <!-- 多次提醒选项 -->
    <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
      <ToggleSwitch x:Name="EnableSameDayReminderToggle"
                    Header="多次提醒"/>
      <ComboBox x:Name="SameDayIntervalComboBox"
                Width="120"
                Margin="8,0,0,0">
        <ComboBoxItem Content="10分钟" Tag="10"/>
        <ComboBoxItem Content="20分钟" Tag="20"/>
        <ComboBoxItem Content="30分钟" Tag="30"/>
        <ComboBoxItem Content="1小时" Tag="60"/>
        <ComboBoxItem Content="2小时" Tag="120"/>
        <ComboBoxItem Content="3小时" Tag="180"/>
        <ComboBoxItem Content="5小时" Tag="300"/>
        <ComboBoxItem Content="6小时" Tag="360"/>
      </ComboBox>
    </StackPanel>
    
    <!-- 确认/取消按钮 -->
    <StackPanel Orientation="Horizontal" 
                HorizontalAlignment="Right" 
                Margin="0,16,0,0">
      <Button Content="取消" Margin="0,0,8,0" Click="CancelReminder_Click"/>
      <Button Content="确认" Style="{StaticResource AccentButton}" Click="ConfirmReminder_Click"/>
    </StackPanel>
  </StackPanel>
  ```

#### 3.3 重复功能
- 现有 MenuFlyout 保持不变
- 新增逻辑: 任务完成时自动创建新任务

### 4. 业务逻辑实现

#### 4.1 截止时间保存
- 修改 `DetailDatePicker_DateChanged` 方法
- 立即保存到数据库
- 更新任务显示

#### 4.2 提醒时间保存
- 创建 `ShowReminderDialog_Click` 方法
- 创建 `ReminderDialog` (ContentDialog 或 FlyoutPresenter)
- 处理多选日期和时间选择
- 保存提醒到数据库

#### 4.3 重复任务自动创建
- 修改 `TaskCheckBox_Click` 方法
- 当任务标记为完成且设置了重复时:
  1. 计算下一个截止日期
  2. 创建新任务（复制原任务内容）
  3. 设置新任务的截止日期和提醒
  4. 更新旧任务的 Recurrence.NextDueDate

#### 4.4 多次提醒生成
- 当设置多次提醒时，根据间隔生成多个提醒记录
- 例如: 设置 10分钟间隔，会生成多个提醒记录

### 5. 数据保存问题修复

#### 5.1 任务标题即时保存
- 修改 `DetailTitle_TextChanged` 方法
- 添加延迟保存（300ms debounce）
- 或在 LostFocus 时保存

#### 5.2 子任务即时保存
- 修改 `AddSubTaskFromInput` 方法
- 确保子任务创建后立即保存到数据库

#### 5.3 备注即时保存
- 修改 `DetailDescription_TextChanged` 方法
- 添加延迟保存机制

#### 5.4 添加数据库更新方法
```csharp
public void UpdateTask(TaskItem task)
{
    using var connection = new SqliteConnection($"Data Source={_dbPath}");
    connection.Open();
    var command = connection.CreateCommand();
    command.CommandText = @"UPDATE Tasks 
                           SET Title = $title, 
                               Description = $description,
                               DueDate = $dueDate
                           WHERE Id = $id";
    command.Parameters.AddWithValue("$title", task.Title);
    command.Parameters.AddWithValue("$description", task.Description ?? (object)DBNull.Value);
    command.Parameters.AddWithValue("$dueDate", task.DueDate?.ToString("o") ?? (object)DBNull.Value);
    command.Parameters.AddWithValue("$id", task.Id);
    command.ExecuteNonQuery();
}
```

### 6. WinUI3 系统提醒组件集成

#### 6.1 使用 ToastNotificationManager
- 引用: `Microsoft.Toolkit.Uwp.Notifications` 或 WinRT API
- 创建提醒服务类 `ReminderService.cs`

#### 6.2 提醒服务实现
```csharp
public class ReminderService
{
    private static ReminderService? _instance;
    public static ReminderService Instance => _instance ??= new ReminderService();
    
    private DispatcherTimer? _checkTimer;
    
    public void StartReminderCheck()
    {
        _checkTimer = new DispatcherTimer();
        _checkTimer.Interval = TimeSpan.FromMinutes(1);
        _checkTimer.Tick += CheckReminders;
        _checkTimer.Start();
    }
    
    private void CheckReminders(object? sender, object e)
    {
        var now = DateTime.Now;
        var dueReminders = _dbService.GetDueReminders(now);
        foreach (var reminder in dueReminders)
        {
            ShowSystemNotification(reminder);
        }
    }
    
    private void ShowSystemNotification(Reminder reminder)
    {
        // 使用 WinUI3 的 ToastNotification 或 Windows.UI.Notifications
        var content = new ToastContentBuilder()
            .AddText("任务提醒")
            .AddText(reminder.TaskTitle)
            .AddText($"截止时间: {reminder.ReminderDateTime}")
            .Show();
    }
}
```

## 文件修改清单

### 需要修改的文件
1. `Models/Reminder.cs` - 扩展提醒模型
2. `Models/Recurrence.cs` - 扩展重复模型
3. `Services/DatabaseService.cs` - 扩展数据库操作
4. `MainWindow.xaml` - 修改截止日期UI，添加提醒弹窗
5. `MainWindow.xaml.cs` - 实现所有事件处理逻辑

### 需要创建的文件
1. `Services/ReminderService.cs` - 提醒服务（使用 WinUI3 系统提醒）

## 实现顺序

1. **第一阶段**: 数据模型和数据库扩展
   - 扩展 Reminder 和 Recurrence 模型
   - 更新数据库表结构
   - 添加新的数据库操作方法

2. **第二阶段**: UI 完善
   - 完善截止日期功能
   - 创建提醒设置弹窗
   - 确保所有 UI 元素正确绑定

3. **第三阶段**: 业务逻辑
   - 实现即时保存功能
   - 实现重复任务自动创建
   - 实现多次提醒生成

4. **第四阶段**: 系统提醒集成
   - 创建 ReminderService
   - 集成 Windows 通知系统
   - 测试提醒功能

## 注意事项

1. **性能**: 使用 debounce 机制避免频繁数据库操作
2. **数据一致性**: 确保 UI 和数据库状态同步
3. **用户体验**: 所有弹窗和选择器应有良好的交互反馈
4. **系统兼容性**: 使用 WinUI3 原生组件确保 Windows 10/11 兼容
