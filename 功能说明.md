# Memo · 待办事项 — 功能说明

> 版本 1.0.7 | WinUI 3 桌面应用 | Windows 10/11

---

## 一、概览

Memo 是一款 Windows 桌面待办事项管理应用，支持任务管理、多级分组、记事本、提醒和重复任务等核心功能，提供桌面固定模式（便签风格）和任务栏托盘运行。

---

## 二、导航结构

左侧导航栏包含三个固定入口：

| 图标 | 名称 | 说明 |
|------|------|------|
| 📒 | **记事本** | 多标签页 Markdown 编辑器 |
| 📋 | **任务** | 四象限矩阵，按重要度×紧急度分布全部未完成任务 |
| &nbsp;&nbsp;└ ⚡ | **即时任务** | 被标记为重要的任务汇总 |
| &nbsp;&nbsp;└ 🔁 | **例行任务** | 日常 / 周常 / 月常内置列表 |

导航栏底部可新建**自定义列表**和**分组**，支持列表嵌套在分组内管理。

---

## 三、核心功能

### 3.1 任务管理

**添加任务**
- 底部输入框输入任务标题，按 Enter 快速添加
- 支持设置**截止日期**（日历选择）
- 支持设置**重复规则**（每天/每周/每月/每年）

**任务列表操作**
- 点击复选框标记完成/未完成
- 右键菜单 → 删除任务
- 点击任务卡片打开右侧详情抽屉
- 已完成任务折叠展示，可展开查看

**详情抽屉**（右侧滑出面板）
- 编辑任务标题
- 编辑**备注**（支持 Markdown 链接 `[title](url)` 语法）
- 关联记事本标签页（双向同步内容）
- **子任务**：多层子任务列表，支持添加、编辑、勾选、删除
- 设置截止日期
- 设置提醒（自定义日期+时间，支持同天重复提醒）
- 设置重复规则
- **优先级**：重要度（高/低）、紧急度（高/低，默认根据截止日期自动判定）
- 截止日提醒开关

### 3.2 任务矩阵（四象限）

基于艾森豪威尔矩阵，用**重要度**和**紧急度**两个维度定位任务：

| 象限 | 条件 | 建议 |
|------|------|------|
| Q1 重要且紧急 | 高重要 + 高紧急 | 立即处理 |
| Q2 重要不紧急 | 高重要 + 低紧急 | 计划安排 |
| Q3 紧急不重要 | 低重要 + 高紧急 | 快速处理 |
| Q4 可暂缓 | 低重要 + 低紧急 | 低优先级 |

**紧急度自动规则**（未手动覆盖时）：
- 无截止日期 → 低
- 已过期 → 高
- 截止日期在本周内（周一至周日）→ 高
- 本周之后 → 低

**设置方式**（任务详情抽屉）：
- 2×2 快捷选择器一键定位象限
- 重要度 / 紧急度独立开关
- 「恢复自动判定」可让紧急度重新跟随截止日期

**任务页面**：展示全部列表中的未完成任务，按象限分栏；点击任务卡片打开详情抽屉。

### 3.3 子任务

- 在任务详情抽屉中管理
- 按 Enter 确认添加一条，输入框清空后可继续添加下一条
- 内联编辑标题
- 复选框独立勾选
- 右键删除

### 3.4 记事本

- 多标签页 Markdown 编辑器
- 支持**预览/编辑**双模式切换
- 快捷键：
  - `Enter` → 从预览切换到编辑
  - `Esc` → 从编辑返回预览
  - `Ctrl+S` → 保存
  - `Q` / `E` → 切换前后标签页
- 支持打开外部 `.md` / `.txt` 文件
- 支持导出保存
- 关闭标签后有 **5 秒撤销** 机会，超时自动删除
- 可关联到任务的备注中实现双向同步

### 3.5 提醒系统

- **截止日提醒**：设置截止日期时自动生成默认提醒（当天 09:00），可手动开关
- **自定义提醒**：支持选择多个日期和时间，可设置当日间隔重复
- 通过 Windows Toast 通知推送
- 应用托盘运行时可正常接收提醒

### 3.6 重复任务

支持四种重复类型：
- 每天
- 每周
- 每月
- 每年

任务完成后自动生成下一次的副本，提醒和备注等属性一并继承。

### 3.7 列表与分组

- **自定义列表**：可按主题分类任务（如"购物"、"工作"）
- **分组**：将多个列表归为一组管理
- 支持重命名、移动至分组、删除等操作
- 内置例行任务列表（日常/周常/月常）不可删除

---

## 四、高级功能

### 4.1 桌面固定模式

- 点击标题栏 📌 按钮将任务列表或记事本**固定到桌面**
- 进入紧凑窗口模式（类似便签/悬浮窗）
- 再次点击恢复为正常窗口
- 快捷键 `Ctrl+Shift+P` 强制退出固定模式（兜底恢复）

### 4.2 系统托盘

- 关闭窗口时最小化到系统托盘（非退出）
- 托盘图标支持快速查看
- 右键菜单：显示主窗口 / 退出

### 4.3 数据库导入/导出

- 导出完整数据库到指定路径
- 导入支持两种模式：
  - **覆盖**：用导入文件替换当前数据库
  - **追加**：按标题去重合并数据

### 4.4 备注关联记事本

- 在任务详情抽屉中可将备注与某个记事本标签页关联
- 任一方的编辑双向同步到另一方
- 可随时取消关联，恢复为独立备注

---

## 五、数据存储

- 使用 SQLite 本地数据库 (`todo.db`)
- 存储位置：`%LOCALAPPDATA%\Packages\<包名>\LocalState\`
- 自动迁移旧版本数据
- 自动完成过期任务（当天截止 17:00 后自动勾选）

---

## 六、开机自启

- 通过 MSIX manifest 注册自启任务
- 默认关闭，可在系统任务管理器 → 启动中启用

---

## 七、技术栈

| 层 | 技术 |
|----|------|
| UI 框架 | WinUI 3 (Windows App SDK 2.0) |
| 数据库 | Microsoft.Data.Sqlite 8.0 |
| 通知 | Windows Toast Notifications |
| 打包 | MSIX (可选不签名部署) |
| 托盘 | H.NotifyIcon.WinUI |
| 目标平台 | Windows 10 17763+ / Windows 11 |
| 语言 | 简体中文 (zh-CN) |
